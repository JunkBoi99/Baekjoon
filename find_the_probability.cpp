#include <iostream>
#include <vector>
using namespace std;

int count;

int solve(vector<int> x)
{
    vector<int> gogo;
    for(int t = 3;t<=x.size();t++)
    {
        for(int k = 0;k)
    }
}

int main()
{
    int n;
    cin >> n;

    vector<int> nums;
    for(int i = 1;i<=n;i++)
        nums.push_back(i);
    solve(nums);
}